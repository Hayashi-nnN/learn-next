/*export const metadata = {
  title: 'MovieInfo layout',
  description: 'MovieInfo layout',
};*/

export default function MovieInfoLayout({ children }) {
  return <div className='movieInfoLayout'>{children}</div>;
}
